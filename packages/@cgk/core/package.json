{
  "name": "@cgk/core",
  "version": "1.0.0",
  "description": "Core constructs for the CGK",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -b .",
    "local": "npm run build && npm link"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "yaml": "^1.10.0"
  },
  "devDependencies": {
    "@types/node": "^14.14.6",
    "nodemon": "^2.0.6",
    "ts-node": "^9.0.0",
    "typescript": "^4.0.5"
  }
}
